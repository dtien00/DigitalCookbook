VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional. Renders a fixed top banner labelling the current environment
# (e.g. "test", "staging"). Leave unset in Production. In Vercel, scope this
# variable to Preview deployments only — see refs/TESTING.md → Environments.
VITE_ENV_LABEL=
